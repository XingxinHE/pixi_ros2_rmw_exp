The talker and the subscriber should be in the same network environment.

Find the network interface via
```shell
ip -br addr
```

Replace this string with the `ROS_NETWORK_INTERFACE` in the `pixi.toml`.
